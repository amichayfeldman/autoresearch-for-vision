[project]
name = "cv-autoresearch"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "albumentations>=1.4",
    "lightning>=2.2",
    "numpy>=1.24",
    "torch>=2.0",
]

[tool.ruff]
line-length = 100
